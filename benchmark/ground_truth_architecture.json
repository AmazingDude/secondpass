{
  "version": "1",
  "description": "Architecture Worker detection-quality benchmark ground truth (separate from Security's ground_truth.json). Cross-worker authz-bleed on Security fixtures is asserted after the run via app.benchmark_cross_worker — those files are not scored here so Architecture precision stays about architecture targets only.",
  "fixtures": {
    "benchmark/fixtures/architecture/checkout_handler.py": [
      {
        "finding_type": "layering_violation",
        "symbol": "checkout",
        "line": 15
      }
    ],
    "benchmark/fixtures/architecture/low_level_persistence_client.py": [
      {
        "finding_type": "dependency_direction",
        "symbol": "write_record",
        "line": 17
      }
    ],
    "benchmark/fixtures/architecture/clean_price_formatter.py": []
  }
}
